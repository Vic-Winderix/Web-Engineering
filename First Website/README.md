# Instructions
This webside has an inplemented game, build in Godot Engine. You will have to complete the following stepts to make the demo game work inside your browser.
## 📦 Requirements

1. **Node.js** must be installed  
   Download it here: https://nodejs.org/

You do **not** need to manually install `local-web-server`, because `npx` will download and run it automatically.

## ▶️ Start the web server

Run the script run.bat, provided in the folder 

## Navigate to provided ip

The script will display a few options to navigate to. Navigate to one of the addresses in the browser and you can now see a folder structure with the webpages which you can visit.
